#include <iostream>
#include <vector>
using namespace std;

vector<vector<string>> allConstruct(string target, vector<string> strs)
{
    vector<vector<string>> ans(target.size() + 1, vector<string>());
    ans[0] = {};
}

int main()
{

    return 0;
}